---
const {title, image, year} = Astro.props;
---

<div class="flex flex-col justify-center items-center">
    <div class="rounded w-52 h-72 border-[5px] border-neutral-300 shadow-lg">
      <figure class="w-full h-full">
        <img src={image} alt={title} class="object-cover w-full h-full"/>
      </figure>
    </div>
    <h1 class="text-xl font-bold text-center text-black mt-2 mb-2">{title}</h1>
    <p class="text-black font-bold ">{year}</p>
</div>


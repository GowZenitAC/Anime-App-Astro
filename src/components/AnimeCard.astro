---
const {title, image, year} = Astro.props;
---

<div class="flex flex-col justify-center items-center">
    <img src={image} alt={title} class="w-32 h-52 object-cover hover:w-32 hover:h-60 hover:transition-all"  />
    <h1 class="text-xl font-bold text-center text-white mt-2 mb-2">{title}</h1>
    <p class="text-white font-bold ">{year}</p>
</div>

